import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alexrc.renzitosstorage',
  appName: 'renzitos-storage',
  webDir: 'www',
};

export default config;
